use bevy::prelude::*;

/// Plugin for the toolbar component. Currently no runtime systems are required.
pub struct ToolbarPlugin;

impl Plugin for ToolbarPlugin {
    fn build(&self, _app: &mut App) {}
}
